请根据以下输入完成一次 Java/Spring Boot 项目升级诊断。

分析时应先识别当前 Java、Spring Boot、构建配置和依赖版本，再结合用户目标与参考资料判断风险、兼容性问题、修改建议和升级顺序。输入没有提供的事实不得自行补造。

<project_context>
$projectContext$
</project_context>

<user_question>
$userQuestion$
</user_question>

<references>
$references$
</references>

按照 System Prompt 规定的 JSON 结构返回最终结果。只输出 JSON，不要输出分析过程。
